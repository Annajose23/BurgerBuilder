import React from 'react';
import classes from '../Burger/'

const BurgerIngredient = (props) => {
        let ingredient = null;

        switch(props.type){
            case('bread-bottom'):
            ingredient = <div className={} 
        }

    
}
 
export default BurgerIngredient;